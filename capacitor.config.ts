import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tharuber.app',
  appName: 'uber',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
